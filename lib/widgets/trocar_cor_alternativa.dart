import 'package:flutter/material.dart';

class TrocarCorAlternativa extends StatelessWidget {
  const TrocarCorAlternativa({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}